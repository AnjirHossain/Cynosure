import store from './store';
// import any kind of api connectivity utils
export default function(onInitComplete) {
	// connect those apis and then call onInitComplete to mount the app
	// to dom

	onInitComplete(null);
}